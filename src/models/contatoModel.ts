export interface Contato {
  id: number;          // Identificador único do contato
  name: string;       // Nome do contato
  telefone: string;   // Telefoneno do contato
  image: string;      // URL da imagem do contato
  email: string;      // Email do contato
}